Convert all flacs in a directory to mp3, the script takes as an argument the directory of the flacs, it creates a new directory where it will contain the mp3s, it will replace the spaces with “_” in the all files.

Example:
	 $  ./convert_flac_mp3.sh directoryFlacs/
